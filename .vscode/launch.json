{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Homepage: Dev",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder:Homepage}"
    },
    {
      "name": "Homepage: Codex Session",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run codex:session",
      "cwd": "${workspaceFolder:Homepage}"
    }
  ]
}
